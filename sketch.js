function setup() {
  createCanvas(600, 400);
  textSize(64)
  fill(255, 0, 0);
}

function draw() {
  background(220);
  text(key, 60, 80);
  text(key, 100,80);
  text(key, 140,80);
  text(key, 180,80);
}